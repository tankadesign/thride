/**
 * Row primitive for the material editor — now the shared inspector `Field`
 * (`ui/widgets/inspector`), re-exported under its historical name so the
 * MapSlot / NoiseEditor / RampEditor call sites keep reading naturally.
 */

export { Field as Row } from "@/ui/widgets/inspector";
